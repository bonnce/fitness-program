---
import Button from './GenericButton.astro';
interface Props {
	text: string;
  className?:string;
  id?:string
}

const { text, className, id } = Astro.props;
const classes = ['py-1 rounded-bl-xl w-full', className ?? '']
---
<Button text={text} {id} className={classes}/>