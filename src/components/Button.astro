---
import GenericButton from './GenericButton.astro';
interface Props {
	text: string;
  className?:string;
  id?:string
}

const { text, className, id } = Astro.props;
const classes = ['px-10 py-4 rounded-xl', className ?? '']
---
<GenericButton text={text} {id} className={classes}/>